# Per-project runtime workspaces — movable with the App (FR-003), never committed.
projects/

# Local env + virtualenv
.env
venv/
.venv/

# DuckDB iteration staging
*.duckdb

# Python
__pycache__/
*.pyc
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
