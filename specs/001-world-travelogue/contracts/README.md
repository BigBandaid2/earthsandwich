# Contracts

This feature does not expose external API contracts. The travelogue is a frontend-only static site with hard-coded data.

Interface expectations are documented in `../data-model.md`, which defines the shape of the itinerary and stop data used by the React application.
